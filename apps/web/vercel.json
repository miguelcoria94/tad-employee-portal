{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @tadhealth/web build",
  "outputDirectory": "dist",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}