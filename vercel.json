{
  "framework": "vite",
  "buildCommand": "pnpm install --frozen-lockfile=false && pnpm --filter @tadhealth/shared build && pnpm --filter @tadhealth/db build && pnpm --filter @tadhealth/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
